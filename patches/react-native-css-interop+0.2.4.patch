diff --git a/node_modules/react-native-css-interop/dist/metro/index.js b/node_modules/react-native-css-interop/dist/metro/index.js
index c91ba6a..e193c44 100644
--- a/node_modules/react-native-css-interop/dist/metro/index.js
+++ b/node_modules/react-native-css-interop/dist/metro/index.js
@@ -176,18 +176,21 @@ async function startCSSProcessor(filePath, platform, isDev, { input, getCSSForPl
                 ? css
                 : getNativeJS((0, css_to_rn_1.cssToReactNativeRuntime)(css, options), debug)));
             debug(`virtualStyles.emit ${platform}`);
+            // Metro >=0.80 changed the FileMap "change" payload to
+            // { changes: { addedFiles, modifiedFiles, removedFiles }, rootDir }.
+            // DeltaCalculator builds the hot-reload delta from the files listed
+            // here, so the regenerated virtual CSS module must be reported as
+            // modified or className/style edits never fast-refresh. The old
+            // { eventsQueue } shape also crashed DependencyGraph._onHasteChange.
             haste.emit("change", {
-                eventsQueue: [
-                    {
-                        filePath,
-                        metadata: {
-                            modifiedTime: Date.now(),
-                            size: 1,
-                            type: "virtual",
-                        },
-                        type: "change",
-                    },
-                ],
+                changes: {
+                    addedFiles: new Map(),
+                    modifiedFiles: new Map([
+                        [filePath, { modifiedTime: Date.now(), size: 1, type: "virtual" }],
+                    ]),
+                    removedFiles: new Map(),
+                },
+                rootDir: "",
             });
         }).then((css) => {
             debug(`virtualStyles.initial ${platform}`);
diff --git a/node_modules/react-native-css-interop/src/metro/index.ts b/node_modules/react-native-css-interop/src/metro/index.ts
index fa58c7b..6aaa9ac 100644
--- a/node_modules/react-native-css-interop/src/metro/index.ts
+++ b/node_modules/react-native-css-interop/src/metro/index.ts
@@ -341,18 +341,23 @@ async function startCSSProcessor(
         );
 
         debug(`virtualStyles.emit ${platform}`);
+        // Metro >=0.80 changed the FileMap "change" payload from { eventsQueue }
+        // to { changes: { addedFiles, modifiedFiles, removedFiles }, rootDir }.
+        // The old shape crashes DependencyGraph._onHasteChange (reads
+        // changes.addedFiles off undefined). It also matters WHAT we report:
+        // DeltaCalculator builds the hot-reload delta from the files listed here,
+        // so we must list the regenerated virtual CSS module as modified —
+        // otherwise className/style edits never fast-refresh. rootDir "" keeps the
+        // (absolute) filePath as the canonical path Metro joins against.
         haste.emit("change", {
-          eventsQueue: [
-            {
-              filePath,
-              metadata: {
-                modifiedTime: Date.now(),
-                size: 1, // Can be anything
-                type: "virtual", // Can be anything
-              },
-              type: "change",
-            },
-          ],
+          changes: {
+            addedFiles: new Map(),
+            modifiedFiles: new Map([
+              [filePath, { modifiedTime: Date.now(), size: 1, type: "virtual" }],
+            ]),
+            removedFiles: new Map(),
+          },
+          rootDir: "",
         });
       }).then((css) => {
         debug(`virtualStyles.initial ${platform}`);
